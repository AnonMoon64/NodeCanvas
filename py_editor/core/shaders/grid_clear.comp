#version 430 core

layout(local_size_x = 256) in;

layout(std430, binding = 2) buffer GridHeadBuffer {
    int gridHead[];
};

uniform int gridTotalCells;

void main() {
    uint i = gl_GlobalInvocationID.x;
    if (i >= gridTotalCells) return;
    gridHead[i] = -1;
}
